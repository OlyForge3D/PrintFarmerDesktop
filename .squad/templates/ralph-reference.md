# Ralph Reference (superseded)

This historical template is not Ralph policy. Active rounds load
`.squad/agents/ralph/loop.md`, then only the conditional reference relevant to
the current action. It must never restore polling, idle-watch behavior, or
unbounded re-scanning.
