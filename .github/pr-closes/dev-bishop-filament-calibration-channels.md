# Closing references — dev-bishop-filament-calibration-channels

This PR adds the main-process IPC channels and HTTP client for the
filament-calibration workflow described in
`.squad/decisions/inbox/vasquez-filament-calibration-reframe.md` and enabled by
upstream `OlyForge3D/PrintFarmer` PR #1952.

It is plumbing (Bishop's scope). The renderer wiring (Dallas) and acceptance
tests (Hicks) land in separate PRs; nothing is user-visible on its own.

No issues are closed by this PR.

```closes

```
