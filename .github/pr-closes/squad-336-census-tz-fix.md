# Closing-reference declaration

This PR is a follow-up fix to `check-census-freshness.mjs` requested by
review of #336's implementation (PR #650), which already closed #336. This
PR closes no issue on its own -- the empty `closes` block below is a
deliberate declaration of that, per `.github/pr-closes/README.md`.

```closes

```
